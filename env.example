# Instagram - Uses SSSInstagram.com (100% FREE)
# No API key needed, no Instagram login required
REQUEST_TIMEOUT=60  # Longer timeout for browser automation

# Limits
MAX_FILE_SIZE_MB=100
REQUEST_TIMEOUT=30

# Railway deployment
PORT=8000
HOST=0.0.0.0
